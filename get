#!/bin/sh
set -e

# This script is meant for quick & easy install via:
#   $ curl -fsSL https://doe.cloud/get | sh

do_install()
{
    sh_c='sh -c'

    $sh_c "curl -fsSL https://doe.cloud/doe_latest.tar.gz | tar -xvz -C ~"
    $sh_c "~/doe/bin/doe _self init"
}

do_install